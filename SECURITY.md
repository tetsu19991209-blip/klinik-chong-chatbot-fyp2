# Security

Do not report real patient information, API keys, account tokens or phone
numbers in a public issue. Revoke and rotate any credential immediately if it
is accidentally committed or shared.

Keep secrets in environment variables, a private secret manager or Google
Colab Secrets. The example configuration contains placeholders only.
